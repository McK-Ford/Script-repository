useful awk commands isn't a script in and of itself, but it's a file containing some syntax information for common awk operations - max value in column, filtering, number of unique occurences in field, conditional replacement

rename.sh is the recipe for renaming files based on a refsheet. Will probably not run as a script often - can just run the while line as a one liner.

mk subset bed takes a random subset of 1% of the provided bed.

Acquire fastqs is self-explanatory, starts from SRAs.